# Dev and test dependencies
-r requirements.txt

pytest>=8.2,<9.0
pytest-asyncio>=0.23,<0.24
pytest-cov>=5.0,<6.0
httpx>=0.27,<0.28    # for FastAPI test client

# Linting / formatting (not enforced in CI but good practice)
ruff>=0.4,<0.5
